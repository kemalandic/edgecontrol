.DS_Store
.build/
# Scripts/package.sh output: archive, exported app, staging tree and the dmg.
build/
.swiftpm/
DerivedData/
*.o
*.dylib
dist/
*.ipa
*.dSYM.zip
*.dSYM
/.paks
.internal/
CLAUDE.md
.superpowers/
*.profraw
*.log
TestPlugins/
TestResults.xcresult

# Generated from project.yml by xcodegen. Every consumer regenerates it before
# use — README, CONTRIBUTING, Scripts/ci-local.sh, Scripts/package.sh and CI all
# run `xcodegen generate` first — so committing it bought nothing and cost a
# merge conflict on every pull request that added a file.
EdgeControl.xcodeproj/
